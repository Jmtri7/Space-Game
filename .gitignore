__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
# PyInstaller build output — everything lands under build/ (see docs/BUILD.md)
build/
.venv/
venv/
.DS_Store

# Player save files - local progress, never committed
saves/

# Runtime logs from launching the game for manual verification
game_stdout.log
game_stderr.log

# Crash dump written by main.py's top-level exception handler
error.txt
